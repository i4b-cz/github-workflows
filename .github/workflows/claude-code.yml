name: Claude Code

on:
  workflow_call:
    inputs:
      allowed-tools:
        description: 'Restrict allowed tools (claude_args format)'
        type: string
        default: ''
      base-branch:
        description: 'Branch to check out and to base new Claude branches on (empty = repository default branch)'
        type: string
        default: ''
      model:
        description: 'Claude model (e.g. claude-opus-5); empty = Claude Code default'
        type: string
        default: ''
      effort:
        description: 'Effort level: low | medium | high | xhigh | max; empty = Claude Code default'
        type: string
        default: ''

    secrets:
      CLAUDE_CODE_OAUTH_TOKEN:
        description: 'Claude Code OAuth token (use this OR ANTHROPIC_API_KEY)'
        required: false
      ANTHROPIC_API_KEY:
        description: 'Anthropic API key (use this OR CLAUDE_CODE_OAUTH_TOKEN)'
        required: false

jobs:
  claude:
    # Only run if @claude is mentioned
    if: |
      (github.event_name == 'issue_comment' && contains(github.event.comment.body, '@claude')) ||
      (github.event_name == 'pull_request_review_comment' && contains(github.event.comment.body, '@claude')) ||
      (github.event_name == 'pull_request_review' && contains(github.event.review.body, '@claude')) ||
      (github.event_name == 'issues' && (contains(github.event.issue.body, '@claude') || contains(github.event.issue.title, '@claude')))

    runs-on: ubuntu-latest

    permissions:
      contents: read
      pull-requests: read
      issues: read
      id-token: write
      actions: read

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 1
          ref: ${{ inputs.base-branch }}

      - name: Run Claude Code
        id: claude
        uses: anthropics/claude-code-action@v1
        with:
          anthropic_api_key: ${{ secrets.ANTHROPIC_API_KEY }}
          claude_code_oauth_token: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}
          base_branch: ${{ inputs.base-branch }}
          additional_permissions: |
            actions: read
          claude_args: >-
            ${{ inputs.model && format('--model {0}', inputs.model) || '' }}
            ${{ inputs.effort && format('--effort {0}', inputs.effort) || '' }}
            ${{ inputs.allowed-tools }}
